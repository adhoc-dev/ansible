---
- name: Instalar paquetes de sistema, drivers
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - htop
      - mc
      - tmux
      - ubuntu-restricted-extras
      - apt-transport-https
      - curl
      - wget
      - openssh-client
      - openssh-server
      - jq
      - tldr
      - ppa-purge
      - locate
      - libxshmfence1

- name: Utilidades de compresión
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - zip
      - gzip
      - tar
      - unrar
      - unzip
      - p7zip-full
      - p7zip-rar

- name: Instalar otras aplicaciones
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - poedit
      - fail2ban
      - ufw
      - vlc
      - ffmpeg

- name: Análisis de Hardware, información del sistema
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - htop
      - inxi
      - hwinfo
      - glances
      - mtr
      - hardinfo
      - stacer

- name: Improve battery performance
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - tlp
      - tlp-rdw
      - acpi-call-dkms
      - powertop

- name: Paquetes varios de entorno gráfico, captura de pantalla, etc.
  apt:
    name: "{{ packages }}"
    state: present
    update_cache: true
  vars:
    packages:
      - indicator-multiload
      - alacarte
      - fonts-firacode
      - gnome-tweak-tool
      - flameshot
